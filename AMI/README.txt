This is a convenience script to build an AMI that includes docker compose and netdata.
The base OS is Ubuntu 18.04 LTS.
Note: you will probably need to update the AMI, you can find the current one from https://cloud-images.ubuntu.com/locator/ec2/

Requirements:
You must have the aws CLI installed and your Access Key ID and Secret Access Key in your environment.

